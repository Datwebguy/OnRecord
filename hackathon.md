# Rails

Team file. Not product copy.
Product name is OnRecord. Do not put the event name in the UI.

## Gate

Sibyl Memory is the store. Delete it and the desk dies.

- Delete tenant_scout → queue empty.
- Fresh process opens the filed task from memory, not from chat.
- Recall beat is one unedited segment with an on-screen timestamp or commit hash.

## What we score on

- Coordination: two tenants, COLD handoff, Clerk blind without files.
- Base: one confirmed ping to a bound address Scout filed in Session A.
- Pitch: three columns, not two chat widgets.
- Anyone can run it. Scene is operator input.

## Demo, 3 minutes

0:00–0:20 Desk. Empty Scene. “Scout files. Clerk only acts on what is on record.”
0:20–0:40 Operator pastes one public repo and/or one wallet they control. Save Scene.
0:40–1:00 Session A. Scout files what that Scene produced. Queue shows the task. Stop process.
1:00–1:10 Cut once if needed.
1:10–2:40 Unedited. Timestamp on. New process. Clerk opens the filed task. Type a name that was never filed. NOT ON RECORD. Open the filed Person. Confirm ping. Hash on the event.
2:40–3:00 “Delete Scout and the queue is empty. This desk is the files.”

Do not use a preloaded Maya, ask_014, or a wallet from the repo.

## Submit later (do not do this first)

- Public GitHub, MIT, real commits
- README: what it does, where memory is load-bearing, where Base is used, how memory made this possible, Prior Work
- 2–5 minute video with the recall beat
- Two public posts only when the desk works
